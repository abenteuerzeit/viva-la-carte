@model VLC.Models.Recipes.Hits

@{
    ViewData["Title"] = "SearchRecipesByQuery";
}
<div id="carouselExampleCaptions" class="carousel slide" data-bs-ride="false">
    <div class="carousel-indicators">
        <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
        <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="1" aria-label="Slide 2"></button>
        <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="2" aria-label="Slide 3"></button>
    </div>
    <div class="carousel-inner">

        @for (int i = 0; i < Model.HitsList.Count; i++)
            {
                var image = Model.HitsList[i].Recipe.Images.Regular;
                if (i == 0)
                {
                    <div class="carousel-item active">
                        <img src="@image.Url" class="d-block w-100" alt="@image.ToString()" width="@image.Width" height="@image.Height">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>@Model.HitsList[i].Recipe.Label</h5>
                            <p>@Model.HitsList[i].Recipe.Calories Calories</p>
                        </div>
                    </div>
                }
                else
                {
                    <div class="carousel-item">
                        <img src="@image.Url" class="d-block w-100" alt="@image.ToString()" width="@image.Width" height="@image.Height">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>@Model.HitsList[i].Recipe.Label</h5>
                            <p>@Model.HitsList[i].Recipe.Calories Calories</p>
                        </div>
                    </div>
                }
            
                
        }
    </div>
    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
    </button>
</div>


    <section>
        <partial name="_RecipeSearchPageNav" model="Model" />
        @for(int i=0; i<Model.HitsList.Count; i++)
        {
            ViewData["hit-iterator-value"] = i;
            @await Html.PartialAsync("_Hit.cshtml", Model.HitsList[i]);
        }
    </section>
